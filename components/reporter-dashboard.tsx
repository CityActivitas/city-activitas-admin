'use client'

import { Header } from "@/components/header"

export function ReporterDashboard() {
  return (
    <div className="min-h-screen bg-gray-100">
      <Header />
      <main className="container mx-auto px-4 pt-24">
        {/* 這裡可以之後添加 reporter 專用的內容 */}
      </main>
    </div>
  )
} 